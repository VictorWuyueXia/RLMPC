# Canonical MATLAB sources

All runnable `.m` files for this portfolio live in this folder (`src/matlab/`).

## Deduplication note

The former `Code/generate_combinations.m` and `_Arhive/TheWork/generate_combinations.m` were **byte-identical**. The portfolio keeps a **single** copy here. The old `Code/` tree (including its nested Git metadata) was removed in favor of this layout.

## Legacy experiments

Older experiments and alternate implementations are under `legacy_archive/the_work_archive/` (previously `_Arhive/TheWork/Archive/`).
